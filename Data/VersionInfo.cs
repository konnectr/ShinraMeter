using System;

namespace Shinra
{
    public static class MeterVersion
    {
        // Central, fixed application version string (replaces updater-based version)
        public const string Value = "3.0.19-classicplus";
    }
}
